<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Sign Up • MommyGlow</title>

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Dancing+Script:wght@700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" />

  <style>
    :root {
      --pink: #e91e63;
      --pink-dark: #c2185b;
      --dark: #1a1a1a;
      --gray: #666;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #fff0f8 0%, #fce4ec 50%, #f8bbd0 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
      position: relative;
      overflow: hidden;
    }

    /* Soft glowing background circles */
    body::before,
    body::after {
      content: '';
      position: absolute;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(233,30,99,0.15) 0%, transparent 70%);
      z-index: 0;
    }
    body::before { width: 550px; height: 550px; top: -180px; left: -180px; }
    body::after  { width: 650px; height: 650px; bottom: -220px; right: -220px; }

    /* Glassmorphic Signup Card */
    .signup-card {
      background: rgba(255, 255, 255, 0.90);
      backdrop-filter: blur(22px);
      -webkit-backdrop-filter: blur(22px);
      border-radius: 34px;
      padding: 50px 45px;
      width: 100%;
      max-width: 440px;
      text-align: center;
      box-shadow: 0 30px 70px rgba(233, 30, 99, 0.28);
      border: 1px solid rgba(255, 255, 255, 0.5);
      position: relative;
      z-index: 1;
      animation: float 7s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-18px); }
    }

    .logo {
      font-family: 'Dancing Script', cursive;
      font-size: 44px;
      font-weight: 700;
      background: linear-gradient(90deg, var(--pink), #ff6b9d);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 8px;
    }

    h2 {
      color: var(--pink-dark);
      font-size: 29px;
      font-weight: 600;
      margin-bottom: 8px;
    }

    .tagline {
      color: var(--gray);
      font-size: 15.5px;
      margin-bottom: 35px;
    }

    /* Input Groups with Icons */
    .input-group {
      position: relative;
      margin-bottom: 24px;
    }

    input {
      width: 100%;
      padding: 17px 20px 17px 52px;
      border: 2px solid #fad0e0;
      border-radius: 18px;
      font-size: 16px;
      background: #fff;
      transition: all 0.35s ease;
      outline: none;
    }

    input:focus {
      border-color: var(--pink);
      box-shadow: 0 0 0 5px rgba(233, 30, 99, 0.18);
    }

    .input-group i {
      position: absolute;
      left: 20px;
      top: 50%;
      transform: translateY(-50%);
      color: var(--pink);
      font-size: 19px;
    }

    /* Dark Premium Button */
    button {
      width: 100%;
      padding: 18px;
      background: var(--dark);
      color: white;
      border: none;
      border-radius: 18px;
      font-size: 18px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.4s ease;
      margin-top: 15px;
      box-shadow: 0 12px 35px rgba(0,0,0,0.35);
    }

    button:hover {
      background: #000;
      transform: translateY(-5px);
      box-shadow: 0 20px 50px rgba(0,0,0,0.5);
    }

    /* Error/Success Message */
    .message {
      background: #ffebee;
      color: #c62828;
      padding: 14px;
      border-radius: 14px;
      margin-bottom: 22px;
      font-size: 14.5px;
      border-left: 5px solid var(--pink);
    }

    /* Login Link */
    .login-link {
      margin-top: 28px;
      font-size: 15.5px;
      color: var(--gray);
    }

    .login-link a {
      color: var(--pink);
      font-weight: 600;
      text-decoration: none;
    }

    .login-link a:hover {
      text-decoration: underline;
    }

    /* Mobile Friendly */
    @media (max-width: 480px) {
      .signup-card { padding: 40px 30px; }
      .logo { font-size: 38px; }
      h2 { font-size: 26px; }
    }
  </style>
</head>
<body>

  <div class="signup-card">
    <div class="logo">MommyGlow</div>
    <h2>Create Your Account</h2>
    <p class="tagline">Join thousands of happy moms on this beautiful journey</p>

    <% if(message){ %>
      <div class="message"><%= message %></div>
    <% } %>

    <form action="/signup" method="POST">
      <div class="input-group">
        <i class="fas fa-user"></i>
        <input type="text" name="name" placeholder="Full Name" required />
      </div>

      <div class="input-group">
        <i class="fas fa-envelope"></i>
        <input type="email" name="email" placeholder="Email Address" required />
      </div>

      <div class="input-group">
        <i class="fas fa-lock"></i>
        <input type="password" name="password" placeholder="Create Password" required />
      </div>

      <div class="input-group">
        <i class="fas fa-lock"></i>
        <input type="password" name="confirm_password" placeholder="Confirm Password" required />
      </div>

      <button type="submit">Create Account</button>
    </form>

    <div class="login-link">
      Already have an account? <a href="/login">Log In</a>
    </div>
  </div>

</body>
</html>