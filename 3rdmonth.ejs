<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>3rd Month • MommyGlow</title>

  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&family=Dancing+Script:wght@700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css">

  <style>
    :root {
      --pink: #e91e63;
      --pink-dark: #c2185b;
      --pink-glow: #ff6b9d;
      --light-pink: #fce4ec;
      --white: #ffffff;
      --success: #28a745;
      --danger: #d32f2f;
      --warning: #ff9800;
      --info: #1976d2;
      --diet: #ff9800;
      --activity: #2196f3;
      --lifestyle: #9c27b0;
      --mental: #e91e63;
      --medical: #00bcd4;
      --symptoms: #8bc34a;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #fff0f8 0%, #fce4ec 45%, #f8bbd0 100%);
      background-size: 400% 400%;
      animation: gradient 20s ease infinite;
      color: #444;
      min-height: 100vh;
      overflow-x: hidden;
    }
    @keyframes gradient { 0%{background-position:0% 50%} 50%{background-position:100% 50%} 100%{background-position:0% 50%} }

    /* Wave Background */
    .wave-bg {
      position: fixed;
      top: 0; left: 0; width: 100%; height: 100%;
      pointer-events: none;
      z-index: -1;
    }
    .wave {
      position: absolute;
      bottom: 0;
      width: 200%;
      height: 40vh;
      background: linear-gradient(transparent, rgba(233,30,99,0.08));
      animation: wave 22s linear infinite;
    }
    .wave:nth-child(2) { animation-delay: -11s; opacity: 0.6; }
    @keyframes wave { 0% { transform: translateX(0); } 100% { transform: translateX(-50%); } }

    /* Glass Header */
    header {
      position: fixed;
      top: 0; left: 0; right: 0;
      background: rgba(255,255,255,0.94);
      backdrop-filter: blur(25px);
      border-bottom: 1px solid rgba(233,30,99,0.15);
      padding: 18px 60px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      z-index: 9999;
      box-shadow: 0 15px 50px rgba(233,30,99,0.25);
    }
    .logo {
      font-family: 'Dancing Script', cursive;
      font-size: 46px;
      font-weight: 800;
      background: linear-gradient(90deg, var(--pink), #ff8ab3);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .back-btn {
      padding: 12px 30px;
      background: var(--pink);
      color: white;
      border-radius: 50px;
      text-decoration: none;
      font-weight: 600;
      box-shadow: 0 10px 30px rgba(233,30,99,0.3);
      transition: all 0.4s ease;
    }
    .back-btn:hover {
      transform: translateY(-6px) scale(1.05);
      box-shadow: 0 20px 40px rgba(233,30,99,0.45);
    }

    main {
      max-width: 1200px;
      margin: 140px auto 100px;
      padding: 0 20px;
    }

    h1 {
      text-align: center;
      font-size: 3.6rem;
      font-weight: 800;
      background: linear-gradient(90deg, var(--pink-dark), var(--pink), #ff8ab3);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 16px;
      animation: heartbeat 3s ease-in-out infinite;
    }
    @keyframes heartbeat {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.04); }
    }
    .subtitle {
      text-align: center;
      font-size: 1.3rem;
      color: #666;
      max-width: 900px;
      margin: 0 auto 60px;
      font-weight: 500;
      font-style: italic;
      line-height: 1.7;
    }

    /* Floating Section Cards */
    .section {
      background: rgba(255,255,255,0.96);
      backdrop-filter: blur(20px);
      border-radius: 28px;
      padding: 36px 32px;
      margin: 45px 0;
      box-shadow: 0 20px 50px rgba(233,30,99,0.22);
      border: 1px solid rgba(233,30,99,0.15);
      position: relative;
      overflow: hidden;
      transition: all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }
    .section:hover {
      transform: translateY(-20px) scale(1.03);
      box-shadow: 0 45px 100px rgba(233,30,99,0.4);
    }
    .section::before {
      content: '';
      position: absolute;
      top: -50%; left: -50%;
      width: 200%; height: 200%;
      background: radial-gradient(circle, rgba(233,30,99,0.12) 0%, transparent 70%);
      opacity: 0;
      transition: 0.8s;
    }
    .section:hover::before { opacity: 1; }

    h2.section-title {
      font-size: 2.1rem;
      color: var(--pink-dark);
      margin-bottom: 28px;
      font-weight: 700;
      text-align: center;
      position: relative;
    }
    h2.section-title::after {
      content: '';
      width: 120px;
      height: 6px;
      background: linear-gradient(90deg, transparent, var(--pink), transparent);
      display: block;
      margin: 18px auto 0;
      border-radius: 50px;
    }

    /* Do / Don't Grid */
    .flex-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 32px;
      margin-top: 20px;
    }
    .do-box, .avoid-box {
      background: rgba(255,255,255,0.9);
      padding: 28px;
      border-radius: 22px;
      backdrop-filter: blur(12px);
      transition: all 0.4s ease;
    }
    .do-box {
      border-left: 7px solid var(--success);
      background: #f8fff8;
    }
    .avoid-box {
      border-left: 7px solid var(--danger);
      background: #fff8f8;
    }
    .do-box:hover, .avoid-box:hover {
      transform: translateY(-8px);
    }

    .do-box h4, .avoid-box h4 {
      font-size: 1.5rem;
      margin-bottom: 18px;
      text-align: center;
      padding-bottom: 8px;
      border-bottom: 2px solid;
    }
    .do-box h4 { color: var(--success); border-color: var(--success); }
    .avoid-box h4 { color: var(--danger); border-color: var(--danger); }

    ul {
      list-style: none;
    }
    li {
      padding: 12px 0;
      font-size: 1.08rem;
      position: relative;
      padding-left: 44px;
      transition: all 0.3s ease;
    }
    li:hover { transform: translateX(10px); }

    .do-item::before {
      content: "✓";
      position: absolute;
      left: 0;
      top: 10px;
      font-size: 1.7rem;
      color: var(--success);
      animation: pulse 2s infinite;
    }
    .avoid-item::before {
      content: "✕";
      position: absolute;
      left: 0;
      top: 10px;
      font-size: 1.7rem;
      color: var(--danger);
    }
    @keyframes pulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.15); }
    }

    /* Section Color Overrides */
    .diet { border-left-color: var(--diet); }
    .activity { border-left-color: var(--activity); }
    .lifestyle { border-left-color: var(--lifestyle); }
    .mental { border-left-color: var(--mental); }
    .medical { border-left-color: var(--medical); }
    .symptoms { border-left-color: var(--symptoms); }

    /* Tip Box */
    .tip-box {
      background: linear-gradient(135deg, #fff8e1, #ffecb3);
      border-left: 7px solid var(--warning);
      padding: 20px 28px;
      border-radius: 20px;
      margin: 24px 0;
      font-size: 1.1rem;
      font-weight: 600;
      color: #e65100;
      text-align: center;
      box-shadow: 0 10px 30px rgba(255,193,7,0.2);
    }

    /* Emergency & Family Boxes */
    .emergency-box, .family-box {
      background: rgba(255,255,255,0.96);
      backdrop-filter: blur(20px);
      border-radius: 32px;
      padding: 40px;
      margin: 60px 0;
      text-align: center;
      box-shadow: 0 30px 80px rgba(0,0,0,0.15);
      border: 2px solid;
    }
    .emergency-box {
      border-color: #ff5252;
      background: linear-gradient(135deg, #ffebee, #ffcdd2);
    }
    .family-box {
      border-color: #64b5f6;
      background: linear-gradient(135deg, #e3f2fd, #bbdefb);
    }
    .emergency-box h2 { color: #c62828; font-size: 2.2rem; }
    .family-box h2 { color: #1976d2; font-size: 2.2rem; }

    /* Nested Lists */
    .section ul ul {
      padding-left: 20px;
    }
    .section ul ul li {
      padding-left: 20px;
    }
    .section ul ul li::before {
      content: "→";
      color: var(--pink);
      margin-right: 8px;
      font-weight: bold;
    }

    @media (max-width: 768px) {
      header { padding: 16px 20px; flex-direction: column; gap: 12px; }
      h1 { font-size: 2.7rem; }
      main { margin: 120px auto 60px; padding: 0 15px; }
      .flex-grid { grid-template-columns: 1fr; gap: 24px; }
      .section, .emergency-box, .family-box { padding: 28px 24px; }
      h2.section-title { font-size: 1.8rem; }
    }
  </style>
</head>
<body>

<div class="wave-bg">
  <div class="wave"></div>
  <div class="wave"></div>
</div>

<header>
  <div class="logo">MommyGlow</div>
  <div>
    <% if(name){ %>
      <a href="/precautions" class="back-btn">Back to Journey</a>
    <% } else { %>
      <a href="/" class="back-btn">Back to Home</a>
    <% } %>
  </div>
</header>

<main>
  <h1>Third Month of Pregnancy</h1>
  <p class="subtitle">Your baby's face, fingers, toes, and tiny organs are taking shape. The risk of miscarriage is lowering, but your gentle care keeps the magic growing!</p>

  <!-- Diet -->
  <div class="section diet">
    <h2 class="section-title">Diet & Nutrition</h2>
    <div class="flex-grid">
      <div class="do-box">
        <h4>Include These</h4>
        <ul>
          <li class="do-item">Iron-rich: Leafy greens, dates, beetroot</li>
          <li class="do-item">Calcium: Milk, ragi, almonds, sesame</li>
          <li class="do-item">Protein: Eggs, dal, paneer, chicken (fully cooked)</li>
          <li class="do-item">Vitamin C: Oranges, guava, lemon</li>
          <li class="do-item">Whole grains over refined flour</li>
          <li class="do-item">2–3 liters water daily</li>
        </ul>
      </div>
      <div class="avoid-box">
        <h4>Avoid These</h4>
        <ul>
          <li class="avoid-item">Junk, fried, processed foods</li>
          <li class="avoid-item">Excess sugar or bakery items</li>
          <li class="avoid-item">Raw meat or fish</li>
          <li class="avoid-item">Soft/energy drinks</li>
          <li class="avoid-item">Spicy food if causing acidity</li>
        </ul>
      </div>
    </div>
    <div class="tip-box">Continue Folic Acid & start Calcium + Vitamin D if prescribed</div>
  </div>

  <!-- Activity -->
  <div class="section activity">
    <h2 class="section-title">Activity & Exercise</h2>
    <div class="flex-grid">
      <div class="do-box">
        <h4>Safe to Do</h4>
        <ul>
          <li class="do-item">30 minutes daily walking</li>
          <li class="do-item">Prenatal yoga & gentle stretches</li>
          <li class="do-item">Good posture always</li>
        </ul>
      </div>
      <div class="avoid-box">
        <h4>Avoid</h4>
        <ul>
          <li class="avoid-item">Heavy lifting</li>
          <li class="avoid-item">Long-distance travel</li>
          <li class="avoid-item">Running, jumping, contact sports</li>
          <li class="avoid-item">Sudden bending</li>
          <li class="avoid-item">Cross-legged sitting for long</li>
        </ul>
      </div>
    </div>
  </div>

  <!-- Lifestyle -->
  <div class="section lifestyle">
    <h2 class="section-title">Body & Lifestyle</h2>
    <div class="flex-grid">
      <div class="do-box">
        <h4>Helpful Habits</h4>
        <ul>
          <li class="do-item">Sleep 7–9 hours nightly</li>
          <li class="do-item">Sleep on left side</li>
          <li class="do-item">Small, frequent meals for digestion</li>
          <li class="do-item">Loose, breathable clothes</li>
        </ul>
      </div>
      <div class="avoid-box">
        <h4>Avoid</h4>
        <ul>
          <li class="avoid-item">Alcohol, smoking, drugs</li>
          <li class="avoid-item">Chemicals, hair dyes, pesticides</li>
        </ul>
      </div>
    </div>
  </div>

  <!-- Mental -->
  <div class="section mental">
    <h2 class="section-title">Mental Wellness</h2>
    <div class="flex-grid">
      <div class="do-box">
        <h4>Stay Positive</h4>
        <ul>
          <li class="do-item">Embrace joy & gratitude</li>
          <li class="do-item">Share emotions with family</li>
          <li class="do-item">Soothing music or meditation</li>
        </ul>
      </div>
      <div class="avoid-box">
        <h4>Avoid</h4>
        <ul>
          <li class="avoid-item">Stress, worries, conflicts</li>
        </ul>
      </div>
    </div>
  </div>

  <!-- Medical -->
  <div class="section medical">
    <h2 class="section-title">Medical Check-ups</h2>
    <ul>
      <li class="do-item">NT scan & 1st trimester ultrasound</li>
      <li class="do-item">Blood tests:
        <ul>
          <li class="do-item">Thyroid function</li>
          <li class="do-item">Anemia screening</li>
          <li class="do-item">Blood sugar levels</li>
        </ul>
      </li>
      <li class="do-item">Regular BP monitoring</li>
    </ul>
    <div class="tip-box">For persistent nausea, consult doctor for safe remedies</div>
  </div>

  <!-- Symptoms -->
  <div class="section symptoms">
    <h2 class="section-title">Common Symptoms (All Normal!)</h2>
    <ul>
      <li class="do-item">Nausea easing up</li>
      <li class="do-item">Frequent urination</li>
      <li class="do-item">Mood swings</li>
      <li class="do-item">Mild abdominal stretching</li>
      <li class="do-item">Tender breasts</li>
    </ul>
    <p style="color: #558b2f; font-size: 1.2rem; font-weight: 600; text-align: center; margin-top: 20px;"><strong>These are beautiful signs of your baby's growth</strong></p>
  </div>

  <!-- Emergency -->
  <div class="emergency-box">
    <h2 class="section-title">Warning Signs — Seek Help Now</h2>
    <ul style="display:inline-block;text-align:left;max-width:700px;margin-top:24px;font-size:1.15rem;">
      <li class="avoid-item">Heavy bleeding or spotting</li>
      <li class="avoid-item">Intense abdominal cramps</li>
      <li class="avoid-item">High fever</li>
      <li class="avoid-item">Non-stop vomiting leading to dehydration</li>
      <li class="avoid-item">Foul-smelling discharge</li>
      <li class="avoid-item">Sudden hand/face swelling</li>
    </ul>
  </div>

  <!-- Family -->
  <div class="family-box">
    <h2 class="section-title">Family & Partner Support</h2>
    <ul style="display:inline-block;text-align:left;max-width:700px;margin-top:24px;font-size:1.15rem;">
      <li class="do-item">Create a calm, stress-free home</li>
      <li class="do-item">Share household tasks</li>
      <li class="do-item">Remind her of meals & meds</li>
    </ul>
  </div>
</main>

</body>
</html>